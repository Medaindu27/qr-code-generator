# qr-code-generator
A simple Python project that generates QR codes from user details like name, course, and email.
**# QR Code Generator using Python

This project generates a QR code containing user details like name, course, project, and email.

## 📌 Features
- Generate QR code from text data
- Save QR code as image
- Simple and easy to use

## 🛠 Technologies Used
- Python
- qrcode library

## ▶️ How to Run
1. Install required library:
   ```bash
   pip install qrcode[pil]**
